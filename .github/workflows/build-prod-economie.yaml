name: "Build Production Image: economie"

on:
  push:
    tags:
      - "economie-v*"

jobs:
  extract-version:
    name: Extract version from tag
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.tag.outputs.version }}
    steps:
      - name: Extract version
        id: tag
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#economie-}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

  build:
    name: Build economie (prod)
    needs: extract-version
    uses: ./.github/workflows/_build-push.yaml
    with:
      service_name: economie
      context_path: economie
      dockerfile_path: economie/docker/Dockerfile
      image_name: harbor.dyingstar-game.space/dyingstar/economie
      image_tag: ${{ needs.extract-version.outputs.version }}
      additional_tags: latest
      chart_name: service-economie
      trigger_preprod_deploy: false
    secrets:
      HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
      HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}