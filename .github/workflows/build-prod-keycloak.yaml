name: "Build Production Image: keycloak"

on:
  push:
    tags:
      - "keycloak-v*"

jobs:
  extract-version:
    name: Extract version from tag
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.tag.outputs.version }}
    steps:
      - name: Extract version
        id: tag
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#keycloak-}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

  build:
    name: Build keycloak (prod)
    needs: extract-version
    uses: ./.github/workflows/_build-push.yaml
    with:
      service_name: keycloak
      context_path: keycloak
      dockerfile_path: keycloak/docker/Dockerfile
      image_name: harbor.dyingstar-game.space/dyingstar/keycloak
      image_tag: ${{ needs.extract-version.outputs.version }}
      additional_tags: latest
      chart_name: keycloak
      trigger_preprod_deploy: false
    secrets:
      HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
      HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
