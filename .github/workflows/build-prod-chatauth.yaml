name: "Build Production Image: chatauth"

on:
  push:
    tags:
      - "chatauth-v*"

jobs:
  extract-version:
    name: Extract version from tag
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.tag.outputs.version }}
    steps:
      - name: Extract version
        id: tag
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#chatauth-}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

  build:
    name: Build chatauth (prod)
    needs: extract-version
    uses: ./.github/workflows/_build-push.yaml
    with:
      service_name: chatauth
      context_path: chatauth
      dockerfile_path: chatauth/docker/Dockerfile
      image_name: harbor.dyingstar-game.space/dyingstar/chatauth
      image_tag: ${{ needs.extract-version.outputs.version }}
      additional_tags: latest
      chart_name: service-chatauth
      trigger_preprod_deploy: false
    secrets:
      HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
      HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
