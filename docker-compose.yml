services:
    persistance_service:
      build: 
        context: ./persistance/.docker
      restart: always
      user: "1000:1000"
      volumes:
        - .:/app/
        - ~/.ssh/id_rsa:/home/golang/.ssh/id_rsa
      ports:
        - 50051:50051
